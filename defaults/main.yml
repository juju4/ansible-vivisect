---

toolsetdir: /opt/tools

vivisect_version: HEAD
vivisect_pip_module: https://github.com/williballenthin/vivisect/zipball/master

## if undefined, global install, else in corresponding virtualenv
#vivisect_virtualenv_path: /path/to/env
#vivisect_virtualenv_user: user
#vivisect_virtualenv_home: /path/home

